model:
  kernel_size: 3
  num_layers: 7

training:
  batch_size: 6
  learning_rate: 1e-04
  epochs: 50
  alpha: 0.1
  early_stopping_patience: 12
  val_split: 0.10
  split_seed: 123
  use_gpu: true

paths:
  # 使用 txt 转换后的 MS 数据进行训练（确保目录下是 *_txt.h5）
  h5_glob: "./data/converted_h5/nbvt*_txt.h5"
  output_dir: "./results_ms_txt"
  csv_subdir: "csv"

data:
  samples_per_file: 384
  crop_size: [256, 256]
  input_channel: 0
  input_key: "raw_channels"
  target_key: "spectrum"
